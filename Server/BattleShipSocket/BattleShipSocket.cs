

namespace BattleShipSocket;

public class BattleShipSocket 
{
   // private readonly List<> _clients = new();

    private const int port = 9001;
   
    
    public BattleShipSocket()
    {
      //  _websocket = new Websocket("http://localhost", (ushort)port);
    }
    
    
}